# Flask Todo

A minimal Flask Todo application supporting adding tasks, rejecting blank titles, toggling task states, deleting tasks, and displaying task states.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
python app.py
```

Visit http://localhost:5000

## Test

```bash
pytest test_app.py -v
```
