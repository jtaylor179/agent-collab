# Flask Calculator

A minimal Flask calculator supporting add, subtract, multiply, and divide with a simple HTML UI.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
python app.py
```

Visit http://localhost:5000

## Test

```bash
pytest test_app.py -v
```
